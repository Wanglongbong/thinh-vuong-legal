import Link from 'next/link';
import { ArrowRight, CheckCircle2 } from 'lucide-react';
import { SectionHero } from '@/components/section-hero';
import { contracts, services } from '@/lib/site-data';

export const metadata = { title: 'Sáu nhóm dịch vụ' };
export default function ServicesPage() { return <main><SectionHero eyebrow="Dịch vụ pháp lý" title="Một lộ trình, sáu nhóm công việc" description="Dịch vụ được xếp theo vòng đời dự án ví điện tử, từ quyết định đầu tư đến vận hành và phản ứng sự cố."><div className="hero-note"><strong>(*)</strong><span>Đề mục tối thiểu là nội dung cần thiết hoặc bắt buộc phải chuẩn bị.</span></div></SectionHero><section className="content-section"><div className="site-shell service-detail-list">{services.map((service) => { const related = contracts.filter((item) => item.group === service.id); return <article id={service.id} key={service.id} className="service-detail"><div className="service-detail-index">{service.number}</div><div className="service-detail-main"><div className="service-detail-title"><h2>{service.title}</h2>{service.minimum && <span className="tier minimum">Tối thiểu (*)</span>}{service.advanced && <span className="tier advanced">Nâng cao</span>}</div><p>{service.description}</p><div className="outcome-box"><CheckCircle2 /><span><strong>Sản phẩm đầu ra</strong>{service.outcome}</span></div></div><div className="service-related"><span>Tài liệu liên quan</span>{related.length ? related.slice(0, 4).map((item) => <Link href={`/hop-dong/${item.slug}`} key={item.slug}>{item.shortTitle}<ArrowRight /></Link>) : <p>Báo cáo mô hình, sơ đồ chủ thể và danh mục giấy phép.</p>}</div></article>; })}</div></section></main>; }
